class Pokemon {
    number;
    name;
    type;
    types = [];
    photo;
    
    // Novos campos para a tela de detalhes do mockup
    height;
    weight;
    abilities = [];
    stats = {}; // Ex: { hp: 45, attack: 49, ... }
}